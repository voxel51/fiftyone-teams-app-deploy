{{- if .Values.delegatedOperatorJobTemplates.rbac.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "rbac-do-templates.service-account-name" . }}
  namespace: {{ .Values.namespace.name }}
  {{- with .Values.delegatedOperatorJobTemplates.rbac.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    {{- include "rbac-do-templates.service-account-labels" . | nindent 4 }}
automountServiceAccountToken: true
{{- end }}
