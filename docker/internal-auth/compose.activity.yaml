---
# DEPRECATED: Activity Analytics is on by default. The `fiftyone-mq-redis`
# queue and the `activity-worker` now live in every base compose file
# (compose.yaml, compose.plugins.yaml, compose.dedicated-plugins.yaml), and
# FIFTYONE_ACTIVITY_ENABLED defaults to true.
#
# This file is kept, empty, so existing `-f compose.activity.yaml` commands
# keep working. It adds nothing and will be removed in a future release; drop
# it from your `-f` set.
#
# To turn Activity Analytics off, see
# docker/docs/configuring-activity-analytics.md.
services: {}
